services:
  - type: web
    name: skillgaprec
    runtime: python
    packages:
      - tesseract-ocr
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app.web:app
    envVars:
      - key: GEMINI_API_KEY
        sync: false